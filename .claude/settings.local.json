{
  "permissions": {
    "allow": [
      "Bash(cargo fmt)",
      "Bash(cargo clippy --all --all-features)",
      "Bash(cargo test --lib -- tools::builtin::binance)",
      "Bash(git stash:*)"
    ]
  }
}
